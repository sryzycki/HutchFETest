import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'any'
})
export class VersioningService {
  public getAppVersion() {
    // TODO(Samuel): re-hydrate the state on app init
    // TODO(Samuel): add HttpInterceptor to attach the relevant header
  }

  public getDataVersion() {
    // TODO(Samuel): re-hydrate the state on app init
    // TODO(Samuel): add HttpInterceptor to attach the relevant header
  }
}
